# LabelU 服务地址
base_url: "http://113.141.72.253:38000"

token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MTMsInVzZXJuYW1lIjoiemhhbmd4aWFvQDdnLmNvbSIsImV4cCI6MTc3NzAzMjgxN30.JvtR6AYzwSvdZWeVr1M-ENPyXCksV22mFiR8_SUtduY"

target:
  # 要处理的任务 ID
  task_id: 167

  # 从这个 inner_id 对应样本复制 tagTool
  source_inner_id: 361

  # 把 tagTool 写入这个 inner_id 范围内的样本，包含起止值
  target_inner_id_start: 361
  target_inner_id_end: 369

  # 全局标签工具名，脚本会忽略大小写匹配 tagTool / tagtool
  tag_tool_name: "tagtool"

  # 复制 tagTool 时重新生成标签项 id，避免多个样本共用同一批 id
  regenerate_tag_ids: true

request:
  # LabelU 样本列表接口通常从 page=0 开始
  page_start: 0
  page_size: 10
  interval_seconds: 0.05
  timeout_seconds: 15
  max_retries: 2
  verify_ssl: false

output:
  dir: "outputs"
